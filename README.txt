# Jarvis Holographic Companion

1. Edit `.env` with your Gemini & D-ID API keys.
2. Run `npm install`
3. Start the app: `npm start`
4. Open http://localhost:3000

Type something, Jarvis will respond in real time with voice and facial animation.
